#include <stdio.h>
#include "main.h"

/**
 * add - a function that adds two integers and returns the result.
 *o * @i: first integer.
 * @j: second integer.
 * Return: sum of two numbers.
 */

int add(int i, int j)
{
	return (i + j);
}
